import { Canvas, ActionBar } from "@/components";

const App = () => {
  return (
    <div className="relative size-full">
      <Canvas />
      <ActionBar />
    </div>
  );
};

export default App;
